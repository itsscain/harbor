---
name: harbor-v2-lighthouse
description: "Harbor v2 'Lighthouse' redesign — the phased premium/regulation roadmap; what's built vs pending."
metadata:
  type: project
  originSessionId: 3cd03412-7647-4c62-ade9-5099b108cba8
---

Harbor v2 "Lighthouse" is a large premium-redesign + feature roadmap. The full spec lives at
`C:\Users\penda\Downloads\HARBOR_V2_LIGHTHOUSE_SPEC.md` (companion to the in-repo `docs/HARBOR_DESIGN.md` v1).
**Thesis:** Skylight keeps the schedule; Harbor helps the family on the hard days. The moat is the
**regulation layer** (Anchor co-regulation, Tides pattern intelligence, Adaptive Sensory Intensity, Outposts).

**THE ONE RULE (§2.2):** maximum craft, *adaptive* intensity. Marketing site + parent `/app` go fully
cinematic; the **child wall defaults to calm and earns intensity** (its users are sensory-vulnerable kids).
Every kiosk animation must pass "still calm for a calm-profile child mid-rough-day?".

Build order (do NOT start a phase before the prior P0 is solid): A feel → B ChildView flagships →
C logistics parity → D the Helm (/app) → E moat depth → F delight.

**Phase A — Foundation (the feel) — SHIPPED 2026-06-24:**
- Tokens (`app/globals.css`): `--ease-harbor-*` easing, elevation `kobsidian/ksunken/kfloat`, `--shadow-bloom-*`.
- **Harbor Depth materials**: `LivingAmbient.tsx` (time-aware bg; a 60s clock sets `data-daypart` on `.kiosk-root`; 7 dayparts crossfade 90s), `BeaconLight.tsx` (state-bound conic sweep — idle .06 / active .10 tinted to the child accent / night paused ember), `.grain-overlay` (SVG-noise, mix-blend overlay, kills banding). Mounted in `KioskShell` behind a new `relative z-10` content wrapper. Removed HomeView's old `.kiosk-ambient`.
- **Touch contract**: `Pressable.tsx` (`usePress` hook + `Pressable` button) — pointerdown→scale .97 + `hapticTapLight`, spring release via `.pressable` CSS. Wired into `KButton`/`KIconButton`, `StepCard`, ChildView chore cards, ChoresBoard chips.
- **Haptic vocabulary** (`lib/kiosk/feedback.ts` `HAPTIC` map). Completion haptics now `HAPTIC.stepDone/choreDone/routineDone`. (iOS has no Web Vibration API → wall standardizes on Android tablets; calls no-op cleanly.)
- **Adaptive Sensory Intensity** (`lib/kiosk/motion.ts`): `children.settings.sensory` = calm|standard|vivid (no migration — jsonb), multiplier .6/1/1.25; `scaleCount` scales confetti; parent picker on the child page (`updateChildSettings` persists it). The honest answer to "animate everything."
- **Accent ramp** (`lib/kiosk/accent.ts` `accentRamp(hex)`→bg/line/text/glow) — built, ready for Phase B accent-suffusion.

**Phase B — ChildView flagships — SHIPPED 2026-06-24:** **Anchor** (`Anchor.tsx`) — the guided co-regulation engine: recede → 5 box breaths with breath-paced haptics (`HAPTIC.breathIn/Hold/Out`) + spoken cues + a breathing orb (static under reduced-motion) → optional feelings ladder (logs `check_ins`) → non-shaming re-entry; always-present "I need a break" door on every ChildView; CornerTimer "Breathe with me" launches it. **The Voyage** (`Voyage.tsx`) — day-as-a-journey band in ChildView schedule mode (boat advances along waypoints toward the LighthouseMark; lights beacon-gold when home). **Completion chain** (`animate-radial-fill`) — accent radial wipe on step/chore check-off. **Name collision fixed** (toolkit=Calm Tools, session=Anchor). Migration **0030** (`corners.outcome`/`regulation_seconds`) written but NOT applied (Supabase MCP was offline) — **apply it when MCP reconnects**. Remaining B polish: cross-screen +N token-arc + odometer digit-roll (deferred).

**Phase C — logistics parity — IN PROGRESS:** **Quick Capture SHIPPED 2026-06-24** (`lib/ai/capture.ts` vision extractor + `scanCapture`/`applyCapture` in hub-actions + `components/app/QuickCapture.tsx`, button on `/app/calendar`): photo/paste a flyer → AI proposes events/groceries/to-dos → one-tap add. Untrusted-input-safe (forced tool schema, household-scoped, 5MB/image-type caps, key server-side); fixed a critical invalid-date crash + timezone (browser offset + client local `today`). **Two-way Google Calendar sync SHIPPED (code) 2026-06-24** — user provided OAuth creds; GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI set on Vercel (all envs) + `.env.local` (gitignored; secret NEVER in repo/memory). `lib/google/{oauth,sync}.ts`, routes `app/api/google/{connect,callback,sync}`, Settings connect/disconnect/Sync-now (`GoogleSyncButton`), push-on-add + mirror-on-delete in hub-actions. Tokens in `google_calendar` table — NOT in kiosk_snapshot. **ACTIVATION PENDING: user must run migrations 0030 + 0031 in the Supabase SQL editor** (MCP was offline; SQL handed to user). **Could NOT E2E-test the live OAuth round-trip** (no table) — gated by a 19-agent review instead; verify with the user after they connect. iCloud/Outlook deferred (CalDAV / MS app reg).

**Phase D — The Helm (/app redesign) — SHIPPED 2026-06-24:** `components/app/ParentRail.tsx` (desktop persistent left-rail nav; mobile keeps `ParentNav` bottom bar) wired in `layout.tsx` (lg:pl-64, header now mobile-only). `components/app/WallMirror.tsx` — the centerpiece "On the wall right now" hero on the Helm home (dark mini-wall: each child anchor/reset/done-today + next event + dinner; server-derived, household-scoped, refreshes on load). **Groundings → "Reset Days"** restorative rename in `GroundingCard` + mirror (data model/actions unchanged). Reviewed clean. NOTE: `/app` is auth-gated → verified by review, not screenshot. DEFERRED (optional refinement): the child-detail deep restructure (tabs + optimistic editing + live wall-preview) — the child page already has a hero + works, so a blind rewrite wasn't worth the risk.

**Phase E — moat depth — IN PROGRESS (1/2 shipped 2026-06-24):** **Tides** (`lib/ai/tides.ts` + `generateTides` in hub-actions + `components/app/TidesCard.tsx` on the child page) — AI pattern intelligence from corner/Anchor + check-in history; RLS-scoped, AI-key-gated, skips AI when <3 data points; works on demand now, caches once **migration 0032 (tides_insights)** is applied; reviewed clean. **Need-based routine templates** (§9.2.10: ADHD morning / Autism bedtime / Anxiety wind-down / Low-demand day) added to `ROUTINE_TEMPLATES` in actions.ts + a picker group on the child page — NO migration, works now. **Outposts DEFERRED** — needs `device_pairings.kind`+`child_id` and `SECURITY DEFINER` pairing-RPC changes that require the Supabase MCP back to apply + test safely. Also remaining in E: auto-soften sensory, co-parent (`household_members`), Family Goal.

**MIGRATIONS 0030/0031/0032 APPLIED 2026-06-24** (MCP reconnected) — Google sync, Tides caching, Anchor outcome capture now ACTIVE. get_advisors clean (new tables RLS-protected; only the pre-existing by-design SECURITY DEFINER/storage/leaked-password warnings). **Family Goal SHIPPED** (§9.2.9, cooperative jar) — `setFamilyGoal` stores in households.settings (no migration, rides snapshot); `components/kiosk/FamilyGoal.tsx` (Home bar, live combined-stars progress) + `components/app/FamilyGoalCard.tsx` (parent control on /app/store). Verified on the wall.

**Outposts SHIPPED 2026-06-24** (§9.1.4, the Buddy-killer) — migrations 0033 (device_pairings.kind/child_id + rpc_kiosk_pair returns them) + 0034 (widen pairings INSERT RLS to household owner — was admin-only; the review caught that createPairingCode would've been RLS-denied). `OutpostShell.tsx` (locked single-child ChildView w/ hideHome + parent lock), KioskApp routes on kind==='outpost', useKiosk stores kind/outpostChildId, `createPairingCode` + Settings 'Devices' mint UI. Verified E2E (paired outpost→Mia room device, no Home; restored to wall). **GOTCHA: apply_migration (MCP) does NOT write a repo file** — I had to hand-write 0033/0034 .sql files to fix repo/DB drift; always write the migration file too.

**Auto-soften SHIPPED 2026-06-24** (§9.1.3) — local-first: a 'big' feeling at Anchor re-entry (Mad/Scared/Too much) → `useKiosk.softenChild` stamps `state.autoSoften[childId]=today` (persisted, offline) → ChildView runs gentler confetti (intensity 0.6) + a '🌙 Taking it easy today' note; auto-restores next morning via the date stamp. `Anchor` gained `onSoften` (fired on big feelings). Verified on the wall.

**Co-parent SHIPPED 2026-06-24** (§9.2.11) — the LAST flagship. Migrations 0035 (household_members join; owners seeded; household_is_mine widened to owner-OR-member, SECURITY DEFINER bypasses the member lookup so no RLS recursion; owner-only write policy) + 0036 (**review-caught CRITICAL**: child_is_mine/routine_is_mine checked owner_id directly → members were denied routines/rewards/check-ins; routed them through household_is_mine). App: getMyHousehold owner-OR-member; inviteCoParent/removeCoParent (owner-only) in hub-actions; Settings 'Guardians' card; invite uses Supabase inviteUserByEmail (redirectTo /login, mirrors parent-invite) + existing-account listUsers fallback. **VERIFIED at the RLS layer via simulated auth contexts (rolled-back txns): member→full access incl child_is_mine; non-member→denied (no leak); owner→intact; non-owner→cannot escalate (membership insert into another household RLS-blocked).** **GOTCHA: widening household_is_mine is NOT enough — child_is_mine + routine_is_mine are SEPARATE SECURITY DEFINER helpers gating child-scoped tables; must update them too** (caught by the adversarial review, not by me).

**🎉 THE LIGHTHOUSE SPEC IS FULLY BUILT + DEPLOYED.** Phases A–F, every flagship: Anchor, Tides, Outposts, Adaptive Sensory (incl. auto-soften), the Voyage, Quick Capture, Google two-way sync, Family Goal, the Helm, co-parent, sound pack, bedtime, cinematic marketing site. Migrations 0029–0036 applied (all with repo files; lib/database.types.ts regenerated from live). Remaining = optional polish only (streaks-on-wall, Harbor Report). **User to verify on real devices:** Google connect + Tides; the co-parent invite email (needs Supabase SMTP configured).

**Phase F — delight/polish — IN PROGRESS (kiosk-side, MCP-independent):** **Sound pack** SHIPPED (`lib/kiosk/feedback.ts play()` — semantic step/routine/reward/arrive/milestone/transition/error; wired routine-complete='routine', Anchor re-entry='arrive', store='reward'). **Bedtime nightlight + sleep sound machine** SHIPPED (`SleepMode` in Screensaver.tsx = amber nightlight + tap-on brown-noise 'waves' via `startSoundMachine`; matches Buddy's nightlight/sound-machine free). **Marketing site (§11) SHIPPED 2026-06-24** — rebuilt `app/page.tsx` on the thesis (cinematic dark hero "Skylight keeps the schedule. Harbor helps your family — especially on the hard days", privacy strip, hard-day→Anchor section, moat cards, honest Harbor-vs-Skylight comparison table, FAQ, kept WaitlistForm). Public → verified visually in preview. Remaining F: Voyages/streaks (streaks computable from reward_log for /app; on-wall needs snapshot or local-history), Harbor Report polish.

**On-wall streaks SHIPPED 2026-06-24** (Phase F) — local-first (no schema): `lib/kiosk/streak.ts` (activeStreak/nextStreak), `useKiosk.bumpStreak` fires on routine-finish (and chore-only day-clear); `StreakBadge` (beacon-gold 🔥, ≥2 days) on ChildView header + compact on ChoresBoard home rows. Verified on the wall (seeded 3-day renders; completing routine ticked 3→4). **CAUTION LEARNED: the preview kiosk is a LIVE paired wall on the user's real household with plus_active=true — completing routines in tests SYNCS real reward_log/points.** I reverted: deleted the 5 test reward_log rows + restored Mia's points_total to 10 + reset local IndexedDB. For future wall tests, prefer seeding/reading IndexedDB over driving real completions, or clean up after.

**BUILD COMPLETE (2026-06-24).** All Lighthouse phases A–F + every flagship shipped & deployed (see the dated SHIPPED notes above). Migrations 0029–0036 applied with repo files. The historical "pending/MCP-blocked" notes below are SUPERSEDED — kept only for narrative. Optional future polish: Harbor Report (weekly AI summary, /app), iCloud/Outlook calendar (CalDAV/MS app reg), Star Catch redesign to a calm scene.

**Honest calls (§10):** Star Catch minigame → cut or redesign to a calm "evening harbor"; Groundings → **Reset Days** (restorative, de-emphasize privilege-stripping); fix "Calm Corner" name collision (toolkit=Calm Tools, session=Anchor); voice → default tap-to-talk, "no always-on mic" as a privacy weapon vs Buddy.

Related: [[harbor-project]], [[harbor-ai-companion]], [[kiosk-professional-ui]].
